import { useRouteError, Link } from "react-router-dom";

const Assessment = () => {
    const error = useRouteError();

  
    return (
      <>
      
      </>
    );
  };
  
  export default Assessment;
